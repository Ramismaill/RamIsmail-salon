# PostgreSQL to SQLite Migration

## Tasks
- [x] Modify `server.js` to use SQLite (better-sqlite3) instead of PostgreSQL (pg)
- [x] Update `scripts/init_db.js` to use SQLite
- [x] Update `.env` file (remove PostgreSQL URLs)
- [x] Create the SQLite database file with tables and seed data
- [x] Test the application
- [x] Create a tool to view/show database tables (`database_report.html`)
